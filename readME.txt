Ejercicio Robot A:
cd ~/robotica/ros2_ws
source install/setup.bash
ros2 launch robot_a display.launch.py
# Una vez que inicie, se deben mover los valores para que se actualicen en el programa.


Ejercicio Robot Bio:
cd ~/robotica/ros2_ws
source install/setup.bash
ros2 launch robot_bio display.launch.py
# Una vez que inicie, se deben mover los valores y presionar "Mover" para que se actualicen en el programa.
